#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a, b;
    a = 4;
    b = 5;
    printf(" a + b is %d\n", a+b);
    printf(" a - b is %d\n", a-b);
    printf(" a * b is %d\n", a*b);
    printf(" a / b is %d\n", a/b);

    return 0;
}
